<%- include('header') %>

<body>
  <main>
    <!-- Bands / artists -->
    <section id="artistList">
      <h1>Bands / artists</h1>
      <p>Select your favorite artists</p>
    
        <ul>
          <% items.forEach(item => { %>
            <li>
              <img src="<%= item.image %>" alt="<%= item.name %>">
              <p><%= item.name %></p>
            </li>
          <% }); %>
        </ul>
      
      <a href="/upload-picture">Next</a>
    </section>
  </main>
  <script src="js/selectartists.js"></script>
</body>
</html>
